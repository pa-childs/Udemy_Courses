alphabet = "a".."z"

# Ranges are stored in an array (seen with use of p)
p alphabet.first(5)

# Using puts will print letters, not the array.
puts alphabet.first(5)

# Upper and Lower case are stored in array 
alphabet = "A".."z"
puts alphabet.first(40)